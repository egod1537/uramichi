[codex] 2026-02-28 SVG 자산 메모
- 사이드바 닫기 버튼용 `sidebar-close.svg` 아이콘을 추가함.
- 아이콘은 `currentColor` 기반 stroke로 작성해 버튼 텍스트 색상 클래스를 그대로 재사용할 수 있도록 맞춤.
[codex] 2026-02-28 지도 필터 라벨 SVG 자산 메모
- 지도 시간 필터 라벨 대체용 `map-time-filter.svg`를 추가함.
- 지도 핀 아이콘 필터 라벨 대체용 `map-pin-filter.svg`를 추가함.
- 두 SVG 모두 텍스트 대신 아이콘+라인 형태로 제작해 필터 카드 헤더에서 공통 크기(`h-5`)로 사용하도록 맞춤.
[codex] 2026-02-28 SVG 자산 메모
- 핀 아이콘 필터 초기화 버튼용 `filter-reset.svg`를 추가함.
- 아이콘은 `currentColor` stroke 기반으로 작성해 버튼 색상 클래스 상속이 가능하도록 맞춤.
[codex] 2026-02-28 SVG 자산 메모
- 핀 아이콘 필터 초기화 버튼 대체용 `filter-reset-alt.svg`를 추가함.
- 아이콘은 회전 화살표 + 슬라이더 라인 조합으로 제작했고 `currentColor` stroke를 사용해 버튼 색상 클래스를 그대로 상속하도록 맞춤.

[codex] 2026-02-28 지도 시간 필터 SVG 수정 메모
- `map-time-filter.svg`에서 시계 오른쪽 장식 가로줄 3개를 제거해 아이콘을 단순화함.
[codex] 2026-02-28 지도 시간 필터 SVG 밀도 조정 메모
- `map-time-filter.svg`를 외곽 캡슐형 라벨에서 시계 단일 아이콘 형태로 축소해 좌측 헤더 공간 낭비를 줄임.
